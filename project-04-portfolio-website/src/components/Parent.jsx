import Child from "./Child";

const Parent = () => {
    return (
        <div style={{
            backgroundColor: "#f0f0f0",
            padding: "20px",
        }}>
            <Child />
        </div>
    );
}

export default Parent;